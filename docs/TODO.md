# TODO / 改善メモ

## 画像アップロード
- [ ] 画像の自動圧縮・リサイズ（Thumbnailator 等）
- [ ] ファイルの実ファイル保存（現状はファイル名のみDBに保存）
- [ ] ストレージ（S3等）への保存対応

## アップロードサイズ上限
- [ ] application.properties に `spring.servlet.multipart.max-file-size=10MB` を追加

## 自己紹介文編集欄
- 現在の文字数、もしくは上限を200文字とした残りの文字数を表示

## 自己紹介編集画面バリデーション対応
- 画像を選択し、字数制限を超えた状態で保存しようとするとエラーメッセージが表示され再編集になるが、
その際に画像はリセットされずに残るようにする。