<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.springbootapplication.mapper.LearningRecordMapper">

    <select id="findByUserIdAndYearAndMonth"
            resultType="com.spring.springbootapplication.domain.LearningRecord">
        SELECT
            id,
            user_id AS userId,
            category_id AS categoryId,
            learning_year AS learningYear,
            learning_month AS learningMonth,
            item_name AS itemName,
            learning_time_minutes AS learningTimeMinutes,
            created_at AS createdAt,
            updated_at AS updatedAt
        FROM learning_records
        WHERE user_id = #{userId}
          AND learning_year = #{learningYear}
          AND learning_month = #{learningMonth}
        ORDER BY category_id, id
    </select>

    <insert id="insert" parameterType="com.spring.springbootapplication.domain.LearningRecord">
        INSERT INTO learning_records (
            user_id,
            category_id,
            learning_year,
            learning_month,
            item_name,
            learning_time_minutes
        ) VALUES (
            #{userId},
            #{categoryId},
            #{learningYear},
            #{learningMonth},
            #{itemName},
            #{learningTimeMinutes}
        )
    </insert>

    <select id="countByUserIdAndYearAndMonthAndItemName" resultType="int">
        SELECT COUNT(*)
        FROM learning_records
        WHERE user_id = #{userId}
          AND learning_year = #{learningYear}
          AND learning_month = #{learningMonth}
          AND item_name = #{itemName}
    </select>

    <update id="updateLearningTimeMinutes">
        UPDATE learning_records
        SET learning_time_minutes = #{learningTimeMinutes},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND user_id = #{userId}
    </update>

    <delete id="deleteByIdAndUserId">
        DELETE FROM learning_records
        WHERE id = #{id}
          AND user_id = #{userId}
    </delete>
    <!-- チャート用：指定期間の年月×カテゴリごとの学習時間合計 -->
    <select id="sumMinutesByUserIdAndMonthRange"
            resultType="com.spring.springbootapplication.domain.CategoryMonthLearningSummary">
        SELECT
            learning_year AS learningYear,
            learning_month AS learningMonth,
            category_id AS categoryId,
            SUM(learning_time_minutes) AS totalMinutes
        FROM learning_records
        WHERE user_id = #{userId}
        AND (learning_year * 100 + learning_month)
            BETWEEN (#{fromYear} * 100 + #{fromMonth})
                AND (#{toYear} * 100 + #{toMonth})
        GROUP BY learning_year, learning_month, category_id
        ORDER BY learning_year, learning_month, category_id
    </select>
</mapper>