<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.springbootapplication.mapper.CategoryMapper">

    <select id="findAll" resultType="com.spring.springbootapplication.domain.Category">
        SELECT
            id,
            category_name AS categoryName,
            created_at AS createdAt,
            updated_at AS updatedAt
        FROM categories
        ORDER BY id
    </select>
    <select id="findById" resultType="com.spring.springbootapplication.domain.Category">
        SELECT
            id,
            category_name AS categoryName,
            created_at AS createdAt,
            updated_at AS updatedAt
        FROM categories
        WHERE id = #{id}
        LIMIT 1
    </select>
</mapper>